defaults:
  - /ml/test
  - /ml/model: classification
  - /ml/data: tiles
  - /ml/backbone@model.backbone: vgg16
  - _self_

model:
  decode_head:
    _target_: ml.modeling.ClassificationHead

metadata:
  run_name: '📏 Testing: Classification Tiles - ${data.test.mode} - fold ${val_fold}'
