defaults:
  - /experiment/ml/train/mil_bags
  - /dataset/embeddings/ikem/1_virchow2@ikem
  - _self_

val_fold: 5
data:
  train:
    uris: ${ikem.mlflow_uris.embeddings.train}
    mode: nancy_high
  val:
    uris: ${ikem.mlflow_uris.embeddings.train}
    mode: nancy_high
model:
  foundation: virchow2
  num_classes: 4
