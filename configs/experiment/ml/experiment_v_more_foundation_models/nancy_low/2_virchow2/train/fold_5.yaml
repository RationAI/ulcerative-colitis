defaults:
  - /experiment/ml/train/mil_bags
  - /dataset/embeddings/ikem/2_virchow2@ikem
  - _self_

val_fold: 5
data:
  train:
    uris: ${ikem.mlflow_uris.embeddings.train}
    mode: nancy_low
  val:
    uris: ${ikem.mlflow_uris.embeddings.train}
    mode: nancy_low
model:
  foundation: virchow2
  num_classes: 3
