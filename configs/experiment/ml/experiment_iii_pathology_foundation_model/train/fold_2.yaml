defaults:
  - /experiment/ml/train/classification_embeddings
  - /dataset/embeddings/ikem/1_prov-gigapath@ikem
  - _self_

val_fold: 2
data:
  train:
    uris: ${ikem.mlflow_uris.embeddings.train}
    mode: all
  val:
    uris: ${ikem.mlflow_uris.embeddings.train}
    mode: all
