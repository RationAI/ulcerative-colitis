defaults:
  - /ml/train
  - /ml/model: classification
  - /ml/data: embeddings
  - /ml/backbone@model.backbone: identity
  - _self_

model:
  decode_head:
    _target_: ml.modeling.LinearProbe

metadata:
  run_name: 'Classification Embeddings - ${data.train.mode} - fold ${val_fold}'
