defaults:
  - /experiment/ml/train/ordered_regression_tiles
  - /dataset/tiled/ikem/0_512@ikem
  - _self_

val_fold: 5
data:
  train:
    uris: ${ikem.mlflow_uris.tiling.train}
    mode: all
  val:
    uris: ${ikem.mlflow_uris.tiling.train}
    mode: all
