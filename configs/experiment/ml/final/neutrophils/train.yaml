defaults:
  - /experiment/ml/train/mil_bags
  - /dataset/embeddings/ikem/2_virchow2@ikem
  - /dataset/embeddings/ftn/2_virchow2@ftn
  - /dataset/embeddings/knl_patos/2_virchow2@knl_patos
  - _self_

val_fold: null
data:
  train:
    uris:
      - ${ikem.mlflow_uris.embeddings.train}
      - ${ftn.mlflow_uris.embeddings.train}
      - ${knl_patos.mlflow_uris.embeddings.train}
    mode: neutrophils
  val:
    uris:
      - ${ikem.mlflow_uris.embeddings.test_preliminary}
      - ${ftn.mlflow_uris.embeddings.test_preliminary}
      - ${knl_patos.mlflow_uris.embeddings.test_preliminary}
    mode: neutrophils
model:
  foundation: virchow2
  num_classes: 1

metadata:
  run_name: '🏋️‍♂️ Training: MIL Bags - neutrophils - final'
