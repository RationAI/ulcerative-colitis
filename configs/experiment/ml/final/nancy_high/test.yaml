defaults:
  - /experiment/ml/final/nancy_high/train
  - /ml/test
  - /checkpoints/final/nancy_high
  - _self_

data:
  test:
    uris:
      - ${ikem.mlflow_uris.embeddings.test_final}
      - ${ftn.mlflow_uris.embeddings.test_final}
      - ${knl_patos.mlflow_uris.embeddings.test_final}
    mode: nancy_high

metadata:
  run_name: '📏 Testing: MIL Bags - nancy_high - final'

