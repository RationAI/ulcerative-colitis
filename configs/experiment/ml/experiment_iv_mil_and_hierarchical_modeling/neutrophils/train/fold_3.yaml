defaults:
  - /experiment/ml/train/mil_bags
  - /dataset/embeddings/ikem/1_prov-gigapath@ikem
  - _self_

val_fold: 3
data:
  train:
    uris: ${ikem.mlflow_uris.embeddings.train}
    mode: neutrophils
  val:
    uris: ${ikem.mlflow_uris.embeddings.train}
    mode: neutrophils
model:
  foundation: prov-gigapath
  num_classes: 1
