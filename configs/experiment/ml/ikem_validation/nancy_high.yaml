# @package _global_

defaults:
  - /experiment/ml/predict/mil_bags
  - /checkpoints/final/nancy_high
  - /dataset/embeddings/ikem_validation@ikem_validation
  - _self_

data:
  predict:
    uris:
      - ${ikem_validation.mlflow_uris.embeddings.predict}

model:
  foundation: virchow2
  num_classes: 4

metadata:
  run_name: '🎯 Predicting: MIL Bags - nancy_high - ikem validation'
  description: 'Predict nancy_high MIL bag scores on the IKEM validation cohort'

