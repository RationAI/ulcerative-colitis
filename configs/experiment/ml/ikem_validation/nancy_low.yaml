# @package _global_

defaults:
  - /experiment/ml/predict/mil_bags
  - /checkpoints/final/nancy_low
  - /dataset/embeddings/ikem_validation@ikem_validation
  - _self_

data:
  predict:
    uris:
      - ${ikem_validation.mlflow_uris.embeddings.predict}
    mode: null

model:
  foundation: virchow2
  num_classes: 3

metadata:
  run_name: '🎯 Predicting: MIL Bags - nancy_low - ikem validation'
  description: 'Predict nancy_low MIL bag scores on the IKEM validation cohort'

