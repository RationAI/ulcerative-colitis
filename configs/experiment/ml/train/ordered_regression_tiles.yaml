defaults:
  - /ml/train
  - /ml/model: ordered_regression
  - /ml/data: tiles
  - /ml/backbone@model.backbone: vgg16
  - _self_

metadata:
  run_name: '🏋️‍♂️ Training: Ordered Regression Tiles - ${data.train.mode} - fold ${val_fold}'
