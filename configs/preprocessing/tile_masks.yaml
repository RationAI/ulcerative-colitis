# @package _global_

max_concurrent: 64
artifact_path: tile_masks
level: 3

metadata:
  run_name: "🎭 Tile Masks: ${dataset.institution}"
  description: Tile masks for ${dataset.institution} at level ${dataset.level}