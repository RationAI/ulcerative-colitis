_target_: ml.data.DataModule
batch_size: 32
num_workers: 4
weighted_sampling: true
train:
  _target_: ml.data.datasets.Embeddings
  uris: ???
  mode: ???
  val_fold: ${val_fold}
  is_val: false
  thresholds:
    blur: 0.25
    artifacts: 0.25
val:
  _target_: ml.data.datasets.Embeddings
  uris: ???
  mode: ???
  val_fold: ${val_fold}
  is_val: true
  thresholds:
    blur: 0.25
    artifacts: 0.25
test:
  _target_: ml.data.datasets.Embeddings
  uris: ???
  mode: ???
  thresholds:
    blur: 0.25
    artifacts: 0.25
